# Runtime dependencies from imports and framework features; not a tested lockfile.
fastapi
uvicorn
pydantic
jinja2
python-multipart
requests
python-dotenv
certifi
pymongo
motor
passlib[bcrypt]==1.7.4
# Conservative backend constraint for the existing Passlib integration.
bcrypt>=3.2,<4
# Current SDK with Responses API and structured outputs.
openai>=2,<3

# Local source masking; install en_core_web_sm separately (see README).
spacy>=3.8,<4
